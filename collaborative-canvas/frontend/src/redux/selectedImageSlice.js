// import { createSlice } from "@reduxjs/toolkit";

// const selectedImageSlice = createSlice({
//   name: "selectedImage",
//   initialState: null,
//   reducers: {
//     setSelectedImage: (state, action) => action.payload,
//     // updateSelectedImage: (state, action) => {
//     //   if (state.selectedImage && state.selectedImage._id === action.payload._id) {
//     //     state = { ...state.selectedImage, ...action.payload };
//     //   }
//     // },
//     clearSelectedImage: (state) => null,
//   },
// });

// export const { setSelectedImage, updateSelectedImage, clearSelectedImage } =
//   selectedImageSlice.actions;
// export default selectedImageSlice.reducer;
